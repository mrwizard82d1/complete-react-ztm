Breakdown React and ReactDOM libraries
- From scratch

The purpose
- React implements the engine
- ReactDOM integrates React with the browser DOM

React simplified
- ReactDOM simply calls `React.createElement` to build tree
- 
